{
  "recommendations": [
    "ms-python.python", // Python extension (debugger, testing UI)
    "charliermarsh.ruff", // Ruff: linter + formatter + import sorting
    "tamasfe.even-better-toml", // TOML (pyproject.toml) with schema/intellisense
    "usernamehw.errorlens" // Inline diagnostics
  ]
}
